專案/文件標題 (使用 # 號表示 H1 標題)介紹與目的 (使用 ## 號表示 H2 標題)這是一份示範用的 Markdown 文件。Markdown 是一種輕量級的標記語言，目的是讓人們在純文字編輯器中也能輕鬆地寫作，並將文字轉換為結構化的 HTML 格式。核心內容 (使用 --- 產生水平分隔線)清單與列表無序列表 (Unordered List):第一個項目第二個項目子項目 A (使用 Tab 或兩個空格縮排)子項目 B有序列表 (Ordered List):步驟一：初始化專案步驟二：撰寫核心邏輯步驟三：進行單元測試格式化文字這段文字包含粗體字 (使用兩個星號 **)、斜體字 (使用一個星號 *)，以及 粗斜體字。如果你需要引用某人的話，可以使用區塊引用：「優雅的程式碼看起來就像專業散文。」程式碼與區塊你可以用反引號 (```) 來嵌入程式碼或語法：行內程式碼：當我們提到 const apiKey = ""; 時，我們是在討論變數宣告。程式碼區塊：function greet(name) {
    console.log(`哈囉, ${name}!`);
}

greet("Markdown 使用者");
連結與圖片點擊這裡前往 Google (這是一個超連結)圖片範例 (如果您的環境支援外部圖片):!總結Markdown 讓文件的編寫變得既簡單又快速。
